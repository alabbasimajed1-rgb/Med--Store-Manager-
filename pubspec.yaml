name: med_store_manager
description: Medicine and supplies inventory manager
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  intl: ^0.19.0
  flutter_local_notifications: ^17.2.1
  google_fonts: ^6.2.1

flutter:
  uses-material-design: true
